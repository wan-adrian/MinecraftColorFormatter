<idea-plugin>
    <id>net.wanmine.mccolor</id>
    <name>Minecraft Color Formatter</name>
    <vendor url="https://example.com">WanMine</vendor>

    <description><![CDATA[
Syntax highlighting for Minecraft legacy color codes (Bedrock palette) inside Java and Kotlin string literals.

<h3>Features</h3>
<ul>
  <li>Real-time preview of <code>§</code> color codes in string literals</li>
  <li>Supports the Bedrock palette including <code>§0</code>-<code>§f</code> and Bedrock-only material codes (<code>§g</code>, <code>§h</code>, ...)</li>
  <li>Works with both direct section sign <code>§</code> and <code>\u00A7</code> representations in source</li>
</ul>
    ]]></description>

    <change-notes><![CDATA[
<ul>
  <li>2026-05-09: Added color carry-over across string concatenations (e.g. <code>PREFIX + "message"</code>).</li>
  <li>2026-05-09: Added per-project color settings for all codes (<code>§0</code>-<code>§f</code> and materials) using the <code>_global_variables.json</code> format, including import and reset-to-defaults.</li>
  <li>2026-05-08: Initial release.</li>
</ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends optional="true" config-file="net.wanmine.mccolor-kotlin.xml">org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="com.intellij">
        <projectService serviceImplementation="net.wanmine.mccolor.settings.MinecraftColorProjectSettings"/>
        <annotator language="JAVA" implementationClass="net.wanmine.mccolor.rendering.MinecraftColorJavaAnnotator"/>
        <projectConfigurable
                parentId="tools"
                instance="net.wanmine.mccolor.settings.MinecraftColorSettingsConfigurable"
                id="net.wanmine.mccolor.settings"
                displayName="Minecraft Color Formatter"/>
    </extensions>
</idea-plugin>
